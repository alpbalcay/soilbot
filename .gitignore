# Python
.venv/
__pycache__/
*.pyc

# Generated data artifacts (regenerate via `python -m pipeline.run`)
data/raw/
data/logs/
data/covariates/
data/*.duckdb
data/*.duckdb.wal
data/*.gpkg
data/*.parquet
data/.duckdb_extensions/

# ML artifacts (regenerate via `python -m ml.assemble` + `python -m ml.train`)
data/ml/

# venv
.venv/

# Rust extension (soilbot-rs): keep src/Cargo.toml/Cargo.lock, ignore build output
soilbot-rs/target/
*.so

# Logs + run output (structured logs, OCR/B1 progress, watcher state)
logs/

# Claude Code local state
.claude/

# Literature review: commit the Obsidian vault (knowledge graph), ignore the bulky/regenerable
# OA PDFs + JSON/text caches (rebuild via `python -m pipeline.run --litreview`).
litreview/pdfs/
litreview/fulltext/
litreview/metadata/

# OCR gold-set validation: commit the labels + manifest + report, ignore the bulky rendered
# page PNGs (regenerate via `python scripts/gold_sample.py`).
gold/render/

# Generated reports are kept in-repo intentionally (schema_audit.md, REPORT.md)
